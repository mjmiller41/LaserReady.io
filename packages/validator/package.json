{
  "name": "@laserready/validator",
  "version": "0.1.0",
  "description": "Source-agnostic laser cut-file validator: file bytes in, deterministic report out. Framework-free TypeScript; runs unchanged in the browser (Phase 0) and Node (Phase 1 guarantee re-check).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "sideEffects": false,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p test/tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "dxf-parser": "1.1.2",
    "svg-pathdata": "9.0.0"
  }
}
